## Packages
recharts | For data visualization (bar charts, line charts, scatter plots)
framer-motion | For smooth animations and transitions between states
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes

## Notes
The application uses a dark theme by default as requested.
API endpoints are defined in shared/routes.ts and should be accessed via the hooks.
